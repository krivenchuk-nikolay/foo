spring:
  application:
    name: order-service

  jackson:
    serialization.write-dates-as-timestamps: false

eureka:
  client:
    healthcheck.enabled: true
    serviceUrl:
      defaultZone: ${SERVICES_REGISTRY}